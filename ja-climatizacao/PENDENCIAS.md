# Pendências antes de publicar — JA Climatização

- [ ] **Logo**: substituir o placeholder. Colocar o arquivo original em `assets/logo.png` (fundo transparente, boa resolução). O `index.html` já referencia esse caminho no cabeçalho e no rodapé — assim que o arquivo existir, ele aparece automaticamente no lugar do ícone provisório.
- [ ] **Instagram**: confirmar o usuário correto e atualizar o link no rodapé (`index.html`, elemento `#instagramLink`) — trocar `href="#"` pelo link real (ex: `https://instagram.com/usuario`) e remover `data-placeholder="true"` e o texto "(confirmar usuário)".
- [ ] **Cidade / região atendida**: preencher os dois campos editáveis marcados em amarelo — um na seção "Onde atendemos" (`#atendimento`) e outro no rodapé — atualmente com o texto `[ Inserir cidade e regiões atendidas ]`.

## Conteúdo confirmado já incluído
- WhatsApp: (83) 99651-0772 — botão fixo, header e seção final apontam para `https://wa.me/5583996510772`.
- Serviços, diferenciais e textos principais conforme especificado.
- Nenhuma avaliação, certificado, ano de experiência ou número de clientes foi inventado.
